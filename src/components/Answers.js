import React, { Component } from "react";import { View, Text, Image, TouchableOpacity, Platform, Dimensions, I18nManager, KeyboardAvoidingView } from "react-native";import {	Container,	Content,	Header,	Button,	Left,	Body,	Right,	Radio,	Card} from 'native-base'import styles from '../../assets/styles';import {connect} from "react-redux";class Answers extends Component {	constructor(props){		super(props);		this.state={			selectedAns : null,			spinner     : false,		}	}	setAns(id){		this.setState({ selectedAns: id });		this.props.setAns(id);	}	render() {		return (			<View style={{ padding: 10 }}>				{					this.props.answers.map((ans, i) => (						<TouchableOpacity onPress={() => this.setAns(ans.id)} key={i} style={{ flexDirection: 'row', marginVertical: 3 }}>							<Radio color={"#f0ad4e"} selectedColor={"#26336d"} onPress={() => this.setAns(ans.id)} selected={this.state.selectedAns === ans.id} />							<Text style={[styles.textRegular, styles.textRight, { color: '#999', fontSize: 18, marginHorizontal: 5 }]}>{ ans.answer }</Text>						</TouchableOpacity>					))				}			</View>		);	}}const mapStateToProps = ({ auth, profile }) => {	return {		auth    : auth.user,		user    : profile.user	};};export default connect(mapStateToProps, {  })(Answers);